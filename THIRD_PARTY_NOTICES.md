# Third-party notices

The current ActionSeam reference implementation has **no third-party runtime or development dependencies**.

Planned external adapters target public projects, but those packages are not currently dependencies of the ActionSeam reference core.

## Researched upstream targets

- DeepSeek Harness (`deepseek-ai/deepseek-harness`) — MIT — planned runtime adapter; not implemented.
- Invokta (`vinilana/invokta`) — MIT — planned action-target adapter; not implemented.

Exact provenance records live under `adapters/`.
